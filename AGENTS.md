# Repository Instructions

- Do not create or restore `.openai/hosting.json`.
- Do not deploy or publish this project to OpenAI Sites.
- Make changes locally by default. Commit or push only when the user explicitly asks.
- Use the `github` remote for GitHub pushes.
